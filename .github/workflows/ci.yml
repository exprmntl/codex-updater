name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check zsh syntax
        run: zsh -n bin/codex-updater
      - name: Run tests
        run: ./tests/test.sh
